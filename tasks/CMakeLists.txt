# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(hazard_detectorTaskLib)
ADD_LIBRARY(${HAZARD_DETECTOR_TASKLIB_NAME} SHARED 
    ${HAZARD_DETECTOR_TASKLIB_SOURCES})
add_dependencies(${HAZARD_DETECTOR_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${HAZARD_DETECTOR_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${HAZARD_DETECTOR_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${HAZARD_DETECTOR_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${HAZARD_DETECTOR_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${HAZARD_DETECTOR_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${HAZARD_DETECTOR_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${HAZARD_DETECTOR_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${HAZARD_DETECTOR_TASKLIB_HEADERS}
    DESTINATION include/orocos/hazard_detector)

